version: '2'

services:

    orderer0:
        extends:
            file: docker-compose-orderer-base.yml
            service: ordererBase
        environment:
            - ORDERER_GENERAL_ORDERERTYPE=solo
